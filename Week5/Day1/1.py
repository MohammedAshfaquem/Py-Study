# get() is a queryset method in Django ORM.
# It is used when you want to retrieve exactly one object from the database.
# It raises:
# DoesNotExist → if no matching record is found.
# MultipleObjectsReturned → if more than one record matches.

# Model.objects.get(field=value)
